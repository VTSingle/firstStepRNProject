import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text: {
        textAlign: 'center'
    },
    count: {
        textAlign: 'center',
        fontSize: 30,
    },
});
export default styles;